const Car = require('./car')

describe('Car class', () => {
  it('works', () => { // THIS is the test
    // assertions
    expect(2 + 2).toBe(5)
  })
  test('everything working', () => { // THIS is another test
    expect({}).toBe({})
  })
})
